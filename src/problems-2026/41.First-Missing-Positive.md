# 41. First Missing Positive

### O(n) - TS with in-place hashing

## Complexity

- Time: $$O(n)$$
- Space: $$O(1)$$

## Submission

Runtime: 5 ms · Beats 56.45%
Memory: 64.40 MB · Beats 58.87%

## Code

```typescript
function firstMissingPositive(nums: number[]): number {
  const length = nums.length;

  for (let i = 0; i < length; i++) {
    if (nums[i] <= 0 || nums[i] > length) nums[i] = length + 1;
  }

  for (let i = 0; i < length; i++) {
    const num = Math.abs(nums[i]);
    if (num <= length) nums[num - 1] = -Math.abs(nums[num - 1]);
  }

  for (let i = 0; i < length; i++) {
    if (nums[i] > 0) return i + 1;
  }

  return length + 1;
}
```

---

- [https://leetcode.com/u/eddyhdzg/](https://leetcode.com/u/eddyhdzg/)
- [https://leetcode.com/problems/first-missing-positive](https://leetcode.com/problems/first-missing-positive)
